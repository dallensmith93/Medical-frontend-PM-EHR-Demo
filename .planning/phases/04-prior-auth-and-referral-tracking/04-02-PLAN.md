---
phase: 04-prior-auth-and-referral-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/types/scheduler-api.types.ts
  - src/app/services/appointment.service.ts
  - src/app/appointment-scheduler.component.ts
  - src/app/appointment-scheduler.component.html
  - src/app/entity-forms.css
  - src/app/appointment-scheduler.component.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Staff can create or update authorization and referral status from the scheduler workflow without leaving the appointment context.
    - Every visible appointment can show its authorization and referral state, including unresolved blockers.
    - Due dates, expiration dates, and notes are visible in the scheduling workflow for follow-up.
    - Missing, pending, denied, or expired prerequisites are highlighted before the visit in the existing scheduler surfaces.
  artifacts:
    - path: src/app/types/scheduler-api.types.ts
      provides: TypeScript contract for prerequisite records and appointment summaries
      min_lines: 55
    - path: src/app/services/appointment.service.ts
      provides: Angular calls for appointment prerequisite list/create/update actions
      min_lines: 35
    - path: src/app/appointment-scheduler.component.ts
      provides: Scheduler state, labels, forms, and actions for prerequisite management
      min_lines: 360
    - path: src/app/appointment-scheduler.component.html
      provides: Scheduler cards, warnings, and quick actions for prerequisite follow-up
      min_lines: 280
    - path: src/app/appointment-scheduler.component.spec.ts
      provides: Angular regression coverage for prerequisite warnings and update actions
      min_lines: 120
  key_links:
    - from: src/app/services/appointment.service.ts
      to: MedicalDemo.Api/Controllers/AppointmentsController.cs
      via: Appointment-scoped prerequisite GET/POST/PUT endpoints
    - from: src/app/appointment-scheduler.component.ts
      to: src/app/types/scheduler-api.types.ts
      via: Typed prerequisite summaries and update DTOs
    - from: src/app/appointment-scheduler.component.html
      to: src/app/appointment-scheduler.component.ts
      via: Visible warning cards and quick actions driven by server-derived blocker state
---

<objective>
Extend the Angular scheduler so prerequisite records, warnings, and update actions are visible in the existing appointment workflow.

Purpose: Keep the scheduler as the product centerpiece by surfacing authorization and referral blockers where staff already review appointments.
Output: TypeScript prerequisite contracts, appointment service methods, and scheduler UI/actions for prerequisite summaries, follow-up notes, due dates, and status updates.
</objective>

<execution_context>
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-BRIEF.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-RESEARCH.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-VALIDATION.md
@.planning/phases/03-patient-intake-and-digital-forms/03-02-SUMMARY.md
@src/app/types/scheduler-api.types.ts
@src/app/services/appointment.service.ts
@src/app/appointment-scheduler.component.ts
@src/app/appointment-scheduler.component.html
@src/app/entity-forms.css
@src/app/appointment-scheduler.component.spec.ts

<interfaces>
Target backend contract from Plan 04-01:
```csharp
public sealed class AppointmentPrerequisiteSummaryDTO
{
    public bool IsRequired { get; set; }
    public string Status { get; set; } = "notRequired";
    public DateOnly? DueDate { get; set; }
    public DateOnly? ExpiresOn { get; set; }
    public string Notes { get; set; } = string.Empty;
    public bool IsBlocking { get; set; }
}

public class AppointmentResponseDTO
{
    public AppointmentPrerequisiteSummaryDTO AuthorizationSummary { get; set; } = new();
    public AppointmentPrerequisiteSummaryDTO ReferralSummary { get; set; } = new();
    public bool HasPrerequisiteBlocker { get; set; }
}
```

Expected endpoint shapes from Plan 04-01:
```text
GET /api/appointments/{id}/prerequisites
POST /api/appointments/{id}/prerequisites
PUT /api/appointments/prerequisites/{prerequisiteId}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Angular prerequisite contracts and service calls</name>
  <read_first>@src/app/types/scheduler-api.types.ts, @src/app/services/appointment.service.ts, @src/app/appointment-scheduler.component.spec.ts, @.planning/phases/04-prior-auth-and-referral-tracking/04-01-PLAN.md</read_first>
  <files>src/app/types/scheduler-api.types.ts, src/app/services/appointment.service.ts, src/app/appointment-scheduler.component.spec.ts</files>
  <action>Add TypeScript types for `AppointmentPrerequisiteDto`, `AppointmentPrerequisiteSummaryDto`, `CreateAppointmentPrerequisiteDto`, and `UpdateAppointmentPrerequisiteDto`. Extend `AppointmentResponseDto` with `authorizationSummary`, `referralSummary`, and `hasPrerequisiteBlocker`. In `AppointmentService`, add concrete methods `getAppointmentPrerequisites(appointmentId)`, `createAppointmentPrerequisite(appointmentId, dto)`, and `updateAppointmentPrerequisite(prerequisiteId, dto)` using the appointment-scoped backend routes. Keep these calls in the existing appointment service; do not create a second Angular service for Phase 4.</action>
  <acceptance_criteria>`src/app/types/scheduler-api.types.ts` contains the four prerequisite DTO types plus `authorizationSummary`, `referralSummary`, and `hasPrerequisiteBlocker` on `AppointmentResponseDto`; `src/app/services/appointment.service.ts` contains methods named `getAppointmentPrerequisites`, `createAppointmentPrerequisite`, and `updateAppointmentPrerequisite` with route strings matching `/appointments/{id}/prerequisites` and `/appointments/prerequisites/{prerequisiteId}`.</acceptance_criteria>
  <verify>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless --include src/app/appointment-scheduler.component.spec.ts</automated>
  </verify>
  <done>The frontend contract and service layer can load and mutate prerequisite data using the backend routes from Plan 04-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface prerequisite summaries, notes, due dates, and update actions in the scheduler</name>
  <read_first>@src/app/appointment-scheduler.component.ts, @src/app/appointment-scheduler.component.html, @src/app/entity-forms.css, @src/app/services/appointment.service.ts, @src/app/appointment-scheduler.component.spec.ts</read_first>
  <files>src/app/appointment-scheduler.component.ts, src/app/appointment-scheduler.component.html, src/app/entity-forms.css, src/app/appointment-scheduler.component.spec.ts</files>
  <action>Extend the existing scheduler component and template to render prerequisite state in the current month/week/day cards and in the upcoming queue. Add concise status badges for authorization and referral using the vocabulary `notRequired|needed|submitted|approved|denied|expired`, show a blocker warning when either summary is blocking, and expose due dates, expiration dates, and notes on upcoming appointment cards. Add inline actions that let staff create or update prerequisite status to `needed`, `submitted`, `approved`, or `denied` without leaving the scheduler. Preserve the current scheduler layout and visual language in `entity-forms.css`; this phase should extend the current queue and cards instead of introducing a new workflow page.</action>
  <acceptance_criteria>`src/app/appointment-scheduler.component.ts` contains logic referencing `authorizationSummary`, `referralSummary`, and `hasPrerequisiteBlocker`; `src/app/appointment-scheduler.component.html` renders at least two prerequisite badges and one blocker warning area; `src/app/entity-forms.css` contains selector styles for prerequisite badges or warnings; `src/app/appointment-scheduler.component.spec.ts` passes with assertions for warning text, due-date or note rendering, and a status-update service call.</acceptance_criteria>
  <verify>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless --include src/app/appointment-scheduler.component.spec.ts</automated>
  </verify>
  <done>The scheduler shows auth/referral blocker truth, follow-up details, and update actions directly alongside appointments and upcoming visits.</done>
</task>

</tasks>

<verification>
Run `npm test -- --watch=false --browsers=ChromeHeadless --include src/app/appointment-scheduler.component.spec.ts`, then `npm run build`, and manually inspect `/appointments` for auth/referral badges, blocker warnings, and quick actions.
</verification>

<success_criteria>
The frontend slice is complete when the scheduler exposes prerequisite status and follow-up actions in the existing appointment workflow without duplicating backend blocker logic in Angular.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prior-auth-and-referral-tracking/04-02-SUMMARY.md`
</output>
