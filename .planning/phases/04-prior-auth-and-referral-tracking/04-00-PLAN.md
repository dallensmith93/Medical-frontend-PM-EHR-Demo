---
phase: 04-prior-auth-and-referral-tracking
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs
  - MedicalDemo.Api.Tests/AppointmentServiceTests.cs
  - src/app/appointment-scheduler.component.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Phase 4 backend and scheduler changes can be verified by automated checks before prerequisite production code is introduced.
    - Authorization and referral rule coverage exists for create, update, status normalization, expiration, due dates, and notes.
    - Scheduler warning and quick-action behavior has executable regression coverage before the Angular UI is expanded.
  artifacts:
    - path: MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs
      provides: Backend tests for prerequisite create/update/status and blocking rules
      min_lines: 60
    - path: MedicalDemo.Api.Tests/AppointmentServiceTests.cs
      provides: Appointment projection tests for prerequisite summaries and blocker flags
      min_lines: 40
    - path: src/app/appointment-scheduler.component.spec.ts
      provides: Scheduler tests for prerequisite badges, warnings, due dates, and update actions
      min_lines: 80
  key_links:
    - from: MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs
      to: MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs
      via: xUnit rule coverage for prerequisite service behavior
    - from: MedicalDemo.Api.Tests/AppointmentServiceTests.cs
      to: MedicalDemo.Api/Services/AppointmentService.cs
      via: xUnit coverage for appointment prerequisite summary projection
    - from: src/app/appointment-scheduler.component.spec.ts
      to: src/app/appointment-scheduler.component.ts
      via: Angular spec coverage for prerequisite rendering and actions
---

<objective>
Create the Wave 0 validation foundation required by Phase 4 before changing auth/referral production code.

Purpose: Keep the phase Nyquist-compliant so prerequisite workflow behavior is locked in with executable tests instead of placeholder verification.
Output: Runnable backend and Angular regression coverage for prerequisite records, appointment summaries, and scheduler warnings/actions.
</objective>

<execution_context>
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-BRIEF.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-RESEARCH.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-VALIDATION.md
@MedicalDemo.Api.Tests/AppointmentServiceTests.cs
@src/app/appointment-scheduler.component.spec.ts
@MedicalDemo.Api/Services/AppointmentService.cs
@src/app/appointment-scheduler.component.ts
@src/app/appointment-scheduler.component.html

<interfaces>
From MedicalDemo.Api/Services/IAppointmentService.cs:
```csharp
public interface IAppointmentService
{
    IReadOnlyCollection<AppointmentResponseDTO> GetAll();
    AppointmentResponseDTO Create(CreateAppointmentDTO request);
    AppointmentResponseDTO UpdateEligibility(int id, UpdateAppointmentEligibilityDTO request);
}
```

From src/app/types/scheduler-api.types.ts:
```typescript
export interface AppointmentResponseDto {
  id: number;
  patientId: number;
  providerId: number;
  appointmentDate: string;
  durationMinutes: number;
  reason: string;
  eligibilityStatus: EligibilityStatus;
  eligibilityReviewedAt: string | null;
  eligibilityNotes: string;
  intakeStatus: IntakeStatus;
  isIntakeComplete: boolean;
  missingIntakeItems: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend prerequisite service tests for create, update, and status rules</name>
  <read_first>@MedicalDemo.Api.Tests/AppointmentServiceTests.cs, @MedicalDemo.Api/Services/AppointmentService.cs, @MedicalDemo.Api/Repositories/InMemoryAppointmentRepository.cs, @MedicalDemo.Api/Repositories/InMemoryPatientRepository.cs, @.planning/phases/04-prior-auth-and-referral-tracking/04-RESEARCH.md</read_first>
  <files>MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs</files>
  <action>Create `AppointmentPrerequisiteServiceTests` as the dedicated xUnit file for Phase 4 backend rules. Write test cases for creating an `authorization` record and a `referral` record linked to an existing appointment/patient pair, rejecting mismatched patient/appointment linkage, normalizing record statuses to the allowed workflow vocabulary `needed|submitted|approved|denied`, preserving `dueDate`, `expiresOn`, and `notes`, and treating an approval with `expiresOn` before the visit date as an expired blocking summary. Keep the tests targeted to in-memory repositories and `NullLogger`, matching the repo's existing backend service test pattern.</action>
  <acceptance_criteria>`MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs` exists, contains tests mentioning both `authorization` and `referral`, asserts the statuses `needed`, `submitted`, `approved`, and `denied`, and includes at least one assertion covering `DueDate`, `ExpiresOn`, and `Notes` persistence.</acceptance_criteria>
  <verify>
    <automated>dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj --filter "FullyQualifiedName~AppointmentPrerequisiteServiceTests"</automated>
  </verify>
  <done>The repo has executable backend tests that define Phase 4 prerequisite creation, update, status, note, and due-date behavior before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend appointment projection tests for prerequisite summaries and blockers</name>
  <read_first>@MedicalDemo.Api.Tests/AppointmentServiceTests.cs, @MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs, @MedicalDemo.Api/Services/AppointmentService.cs, @.planning/phases/04-prior-auth-and-referral-tracking/04-RESEARCH.md</read_first>
  <files>MedicalDemo.Api.Tests/AppointmentServiceTests.cs</files>
  <action>Extend the existing appointment service tests so they describe the future `AppointmentResponseDTO` prerequisite projection. Add assertions for `authorizationSummary`, `referralSummary`, and a top-level blocker flag or equivalent server-derived field on appointment responses. Cover at minimum: no record returns `notRequired`, a `needed` or `submitted` record produces a blocking summary, an `approved` record stays non-blocking until the summary derives `expired`, and notes/due dates flow into the summary DTO. Do not rewrite the existing intake assertions; append Phase 4 coverage alongside them.</action>
  <acceptance_criteria>`MedicalDemo.Api.Tests/AppointmentServiceTests.cs` contains assertions for `authorization` and `referral` summary data, includes a blocker assertion, and includes a test case whose expected summary status is `expired`.</acceptance_criteria>
  <verify>
    <automated>dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj --filter "FullyQualifiedName~AppointmentServiceTests"</automated>
  </verify>
  <done>The appointment projection contract for prerequisite summaries and blocker logic is executable and ready for backend implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend scheduler specs for prerequisite warnings, due dates, and update actions</name>
  <read_first>@src/app/appointment-scheduler.component.spec.ts, @src/app/appointment-scheduler.component.ts, @src/app/appointment-scheduler.component.html, @src/app/services/appointment.service.ts, @src/app/types/scheduler-api.types.ts</read_first>
  <files>src/app/appointment-scheduler.component.spec.ts</files>
  <action>Extend the existing scheduler component spec to describe the Angular behavior Phase 4 must deliver. Add fixture appointment data with `authorization` and `referral` summary DTOs and cover: visible missing or unresolved prerequisite warnings in week/day/upcoming surfaces, due-date and notes display for follow-up, and at least one update action that calls the appointment service with a Phase 4 prerequisite status change such as `submitted` or `approved`. Keep the tests aligned with the current scheduler component and its service-spy setup rather than inventing a second UI entry point.</action>
  <acceptance_criteria>`src/app/appointment-scheduler.component.spec.ts` contains prerequisite-related fixture fields, includes at least one DOM assertion for prerequisite warning text, includes at least one assertion for due-date or notes rendering, and includes at least one expectation that a prerequisite update service method is called.</acceptance_criteria>
  <verify>
    <automated>npm test -- --watch=false --browsers=ChromeHeadless --include src/app/appointment-scheduler.component.spec.ts</automated>
  </verify>
  <done>The Angular scheduler has executable regression coverage for prerequisite warnings and status actions before the production UI is expanded.</done>
</task>

</tasks>

<verification>
Run `dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj --filter "FullyQualifiedName~AppointmentPrerequisite|FullyQualifiedName~AppointmentServiceTests"` and `npm test -- --watch=false --browsers=ChromeHeadless --include src/app/appointment-scheduler.component.spec.ts`.
</verification>

<success_criteria>
Wave 0 is complete when backend and Angular tests concretely define the prerequisite record workflow, appointment summary projection, and scheduler rendering/actions that the implementation plans must satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prior-auth-and-referral-tracking/04-00-SUMMARY.md`
</output>
