---
phase: 04-prior-auth-and-referral-tracking
plan: 01
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - MedicalDemo.Api/Models/AppointmentPrerequisite.cs
  - MedicalDemo.Api/DTOs/AppointmentPrerequisiteDto.cs
  - MedicalDemo.Api/DTOs/CreateAppointmentPrerequisiteDTO.cs
  - MedicalDemo.Api/DTOs/UpdateAppointmentPrerequisiteDTO.cs
  - MedicalDemo.Api/DTOs/AppointmentPrerequisiteSummaryDTO.cs
  - MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs
  - MedicalDemo.Api/Repositories/IAppointmentPrerequisiteRepository.cs
  - MedicalDemo.Api/Repositories/InMemoryAppointmentPrerequisiteRepository.cs
  - MedicalDemo.Api/Services/IAppointmentPrerequisiteService.cs
  - MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs
  - MedicalDemo.Api/Services/IAppointmentService.cs
  - MedicalDemo.Api/Services/AppointmentService.cs
  - MedicalDemo.Api/Controllers/AppointmentsController.cs
  - MedicalDemo.Api/Program.cs
  - MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs
  - MedicalDemo.Api.Tests/AppointmentServiceTests.cs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Staff can create and update one active authorization record and one active referral record per appointment from the API.
    - Appointment responses expose prerequisite summaries with statuses that distinguish `notRequired`, `needed`, `submitted`, `approved`, `denied`, and derived `expired`.
    - Appointments report when an unresolved prerequisite is blocking the visit workflow.
    - Due dates, expiration dates, and notes are persisted on prerequisite records and returned to API consumers.
  artifacts:
    - path: MedicalDemo.Api/Models/AppointmentPrerequisite.cs
      provides: Shared persisted prerequisite aggregate keyed by appointment, patient, and kind
      min_lines: 25
    - path: MedicalDemo.Api/DTOs/AppointmentPrerequisiteSummaryDTO.cs
      provides: Appointment-facing prerequisite summary contract
      min_lines: 15
    - path: MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs
      provides: Create/update/list rules, status normalization, and blocking logic
      min_lines: 90
    - path: MedicalDemo.Api/Services/AppointmentService.cs
      provides: Appointment projection with prerequisite summaries and blocker flag
      min_lines: 110
    - path: MedicalDemo.Api/Controllers/AppointmentsController.cs
      provides: Appointment-scoped prerequisite create/list/update endpoints
      min_lines: 70
  key_links:
    - from: MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs
      to: MedicalDemo.Api/Repositories/IAppointmentPrerequisiteRepository.cs
      via: Repository-backed create and update operations for active prerequisite records
    - from: MedicalDemo.Api/Services/AppointmentService.cs
      to: MedicalDemo.Api/DTOs/AppointmentPrerequisiteSummaryDTO.cs
      via: Summary projection for authorization and referral blockers
    - from: MedicalDemo.Api/Controllers/AppointmentsController.cs
      to: MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs
      via: Appointment-scoped endpoints for create, list, and update actions
---

<objective>
Implement the backend source of truth for authorizations and referrals and project it onto appointments for scheduler consumption.

Purpose: Keep prerequisite workflow state in one backend aggregate so Angular, later claim-readiness work, and future task generation all consume the same blocker truth.
Output: A shared prerequisite model/repository/service, appointment-scoped API endpoints, and enriched appointment responses with prerequisite summaries.
</objective>

<execution_context>
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dallen/Documents/Medical-frontend-PM-EHR-Demo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-BRIEF.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-RESEARCH.md
@.planning/phases/04-prior-auth-and-referral-tracking/04-VALIDATION.md
@.planning/phases/03-patient-intake-and-digital-forms/03-01-SUMMARY.md
@MedicalDemo.Api/Program.cs
@MedicalDemo.Api/Controllers/AppointmentsController.cs
@MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs
@MedicalDemo.Api/Repositories/InMemoryAppointmentRepository.cs
@MedicalDemo.Api/Repositories/InMemoryPatientRepository.cs
@MedicalDemo.Api/Services/IAppointmentService.cs
@MedicalDemo.Api/Services/AppointmentService.cs
@MedicalDemo.Api.Tests/AppointmentServiceTests.cs

<interfaces>
From MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs:
```csharp
public class AppointmentResponseDTO
{
    public int Id { get; set; }
    public int PatientId { get; set; }
    public int ProviderId { get; set; }
    public DateTime AppointmentDate { get; set; }
    public int DurationMinutes { get; set; }
    public string Reason { get; set; } = string.Empty;
    public string EligibilityStatus { get; set; } = "pending";
    public DateTime? EligibilityReviewedAt { get; set; }
    public string EligibilityNotes { get; set; } = string.Empty;
    public string IntakeStatus { get; set; } = "notStarted";
    public bool IsIntakeComplete { get; set; }
    public IReadOnlyCollection<string> MissingIntakeItems { get; set; } = [];
}
```

From MedicalDemo.Api/Controllers/AppointmentsController.cs:
```csharp
[Route("api/[controller]")]
public class AppointmentsController(IAppointmentService appointmentService) : ControllerBase
{
    [HttpGet]
    public ActionResult<IReadOnlyCollection<AppointmentResponseDTO>> GetAppointments() => Ok(appointmentService.GetAll());

    [HttpPut("{id:int}/eligibility")]
    public ActionResult<AppointmentResponseDTO> UpdateEligibility(int id, [FromBody] UpdateAppointmentEligibilityDTO request)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared appointment prerequisite aggregate and backend CRUD contracts</name>
  <read_first>@MedicalDemo.Api/Program.cs, @MedicalDemo.Api/Controllers/AppointmentsController.cs, @MedicalDemo.Api/Repositories/IAppointmentRepository.cs, @MedicalDemo.Api/Repositories/InMemoryAppointmentRepository.cs, @MedicalDemo.Api/Repositories/InMemoryPatientRepository.cs, @MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs</read_first>
  <files>MedicalDemo.Api/Models/AppointmentPrerequisite.cs, MedicalDemo.Api/DTOs/AppointmentPrerequisiteDto.cs, MedicalDemo.Api/DTOs/CreateAppointmentPrerequisiteDTO.cs, MedicalDemo.Api/DTOs/UpdateAppointmentPrerequisiteDTO.cs, MedicalDemo.Api/Repositories/IAppointmentPrerequisiteRepository.cs, MedicalDemo.Api/Repositories/InMemoryAppointmentPrerequisiteRepository.cs, MedicalDemo.Api/Services/IAppointmentPrerequisiteService.cs, MedicalDemo.Api/Services/AppointmentPrerequisiteService.cs, MedicalDemo.Api/Controllers/AppointmentsController.cs, MedicalDemo.Api/Program.cs, MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs</files>
  <action>Create one shared `AppointmentPrerequisite` aggregate for both workflow kinds with fields `Id`, `AppointmentId`, `PatientId`, `Kind`, `Status`, `DueDate`, `ExpiresOn`, and `Notes`. Add request/response DTOs that use `authorization|referral` for `Kind` and persisted statuses `needed|submitted|approved|denied`. Implement repository and service logic that allows one active record per `(appointmentId, kind)`, verifies the appointment exists and matches the supplied patient, normalizes status values, and supports appointment-scoped create/list plus record-id update operations. Register the repository/service in `Program.cs` and extend `AppointmentsController` with concrete routes `GET /api/appointments/{id}/prerequisites`, `POST /api/appointments/{id}/prerequisites`, and `PUT /api/appointments/prerequisites/{prerequisiteId}`. Do not add a separate controller or copy prerequisite fields onto `Appointment`.</action>
  <acceptance_criteria>`MedicalDemo.Api/Models/AppointmentPrerequisite.cs` contains `Kind`, `Status`, `DueDate`, `ExpiresOn`, and `Notes`; `AppointmentsController.cs` contains routes matching `/api/appointments/{id}/prerequisites` and `/api/appointments/prerequisites/{prerequisiteId}`; `Program.cs` registers `IAppointmentPrerequisiteRepository` and `IAppointmentPrerequisiteService`; `AppointmentPrerequisiteServiceTests.cs` passes against the implementation.</acceptance_criteria>
  <verify>
    <automated>dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj --filter "FullyQualifiedName~AppointmentPrerequisiteServiceTests"</automated>
  </verify>
  <done>The API can create, list, and update appointment-linked authorization/referral records through one backend workflow implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project prerequisite summaries and blockers onto appointment responses</name>
  <read_first>@MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs, @MedicalDemo.Api/Services/IAppointmentService.cs, @MedicalDemo.Api/Services/AppointmentService.cs, @MedicalDemo.Api/Services/PatientService.cs, @MedicalDemo.Api.Tests/AppointmentServiceTests.cs, @MedicalDemo.Api.Tests/AppointmentPrerequisiteServiceTests.cs</read_first>
  <files>MedicalDemo.Api/DTOs/AppointmentPrerequisiteSummaryDTO.cs, MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs, MedicalDemo.Api/Services/IAppointmentService.cs, MedicalDemo.Api/Services/AppointmentService.cs, MedicalDemo.Api.Tests/AppointmentServiceTests.cs</files>
  <action>Add `AppointmentPrerequisiteSummaryDTO` and extend `AppointmentResponseDTO` with `AuthorizationSummary`, `ReferralSummary`, and `HasPrerequisiteBlocker`. In `AppointmentService`, read the linked active prerequisite records and derive summary statuses as follows: no record returns `notRequired`; stored `needed`, `submitted`, `approved`, and `denied` flow through; an `approved` record with `ExpiresOn` earlier than the appointment date returns summary status `expired`; `IsBlocking` is true for `needed`, `submitted`, `denied`, and `expired`. Include `DueDate`, `ExpiresOn`, and `Notes` on the summary DTO so Angular does not need a second fetch to render follow-up information. Preserve the existing eligibility and intake projection behavior intact.</action>
  <acceptance_criteria>`MedicalDemo.Api/DTOs/AppointmentResponseDTO.cs` contains `AuthorizationSummary`, `ReferralSummary`, and `HasPrerequisiteBlocker`; `MedicalDemo.Api/Services/AppointmentService.cs` contains logic for `notRequired` and `expired`; `AppointmentServiceTests.cs` includes passing assertions for blocking `needed|submitted|denied|expired` states and non-blocking `approved` state.</acceptance_criteria>
  <verify>
    <automated>dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj --filter "FullyQualifiedName~AppointmentServiceTests"</automated>
  </verify>
  <done>`GET /api/appointments` returns prerequisite summaries and blocker truth directly on each appointment for scheduler consumption.</done>
</task>

</tasks>

<verification>
Run `dotnet test MedicalDemo.Api.Tests/MedicalDemo.Api.Tests.csproj` and `dotnet build MedicalDemo.Api/MedicalDemo.Api.csproj` after the backend changes.
</verification>

<success_criteria>
The backend slice is complete when the API stores appointment-linked auth/referral records and every appointment response includes server-derived prerequisite summaries plus a single blocker flag.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prior-auth-and-referral-tracking/04-01-SUMMARY.md`
</output>
